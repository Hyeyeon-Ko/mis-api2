spring:
  application:
    name: mis
  datasource:
    url: jdbc:mariadb://10.1.10.216:3306/mis  # ???DB ?? URL
    username: user  # ?????? ??? ??
    password: kmi12345  # ?????? ????
    driver-class-name: org.mariadb.jdbc.Driver  # ???DB JDBC ???? ???
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect  # JPA ?????? ??? ??
    hibernate:
      ddl-auto: update  # Hibernate ?? DDL ?? (??)
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
external:
  login:
    url: http://172.16.250.85/mis/login
  userInfo:
    url: http://172.16.250.85/mis/userDeptInfo

file:
  upload-dir: uploads/

logging:
  level:
    org:
      springframework:
        transaction: info
        orm:
          jpa: info
        jdbc:
          core:
            JdbcTemplate: debug
            StatementCreatorUtils: trace
      hibernate:
        type:
          descriptor:
            sql: info
    web: info
    root: info

server:
  port: 9090

