spring:
  application:
    name: mis
  datasource:
    url: jdbc:mariadb://10.1.10.216:3306/mis  # ???DB ?? URL
    username: user  # ?????? ??? ??
    password: kmi12345  # ?????? ????
    driver-class-name: org.mariadb.jdbc.Driver  # ???DB JDBC ???? ???
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect  # JPA ?????? ??? ??
    hibernate:
      ddl-auto: update  # Hibernate ?? DDL ?? (??)
  mail:
    host: smtp.gmail.com
    port: 587
    username: khy33355@gmail.com
    password: lhvwwazvwxoxgekp
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
          mime:
            charset: UTF-8
          transport:
            protocol: smtp
    default-encoding: UTF-8
external:
  login:
    url: http://172.16.250.85/mis/login
  userInfo:
    url: http://10.1.10.129/mis/userDeptInfo

logging:
  level:
    org:
      springframework:
        transaction: info
        orm:
          jpa: info
        jdbc:
          core:
            JdbcTemplate: debug
            StatementCreatorUtils: trace
      hibernate:
        type:
          descriptor:
            sql: info
    web: info
    root: info

server:
  port: 9090
